# Telegram client (kurigram — поддерживаемый форк pyrogram)
# All versions pinned with == for reproducible Docker builds.
kurigram==2.2.9
tgcrypto==1.2.5

# Database
asyncpg==0.30.0

# Configuration (через core.settings)
environs==11.2.1

# Prometheus instrumentation (/metrics на 9100, экспорт common.metrics)
prometheus_client==0.26.0

# Timezone data
tzdata==2024.1
